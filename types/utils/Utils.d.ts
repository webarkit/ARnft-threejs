export declare class Utils {
    constructor();
    private static trackedMatrix;
    private static interpolationFactor;
    static interpolate(world: any): any;
    static isMobile(): boolean;
    static setMatrix(matrix: any, value: any): void;
}
